CREATE TABLE request_land_details (
    id INT(11) NOT NULL AUTO_INCREMENT,
    request_id INT(11) NOT NULL,
    declared_owner VARCHAR(255) NOT NULL,
    owner_address VARCHAR(255) NOT NULL,
    property_location VARCHAR(255) DEFAULT NULL,
    title VARCHAR(255) NOT NULL,
    lot VARCHAR(255) NOT NULL,
    arp_no VARCHAR(255) NOT NULL,
    area VARCHAR(255) NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY idx_request_id (request_id),
    CONSTRAINT fk_request_land_details_request
        FOREIGN KEY (request_id) REFERENCES requests(id)
        ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE DATABASE IF NOT EXISTS db_tax_archive
CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;

USE db_tax_archive;

CREATE TABLE IF NOT EXISTS import_batches (
    id INT AUTO_INCREMENT PRIMARY KEY,
    batch_name VARCHAR(255) NOT NULL,
    source_file VARCHAR(255) NOT NULL,
    remarks VARCHAR(255) DEFAULT NULL,
    total_rows INT NOT NULL DEFAULT 0,
    inserted_rows INT NOT NULL DEFAULT 0,
    skipped_rows INT NOT NULL DEFAULT 0,
    duplicate_rows INT NOT NULL DEFAULT 0,
    imported_by VARCHAR(100) DEFAULT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS taxpayer_raw_imports (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    batch_id INT NOT NULL,
    row_num INT NOT NULL,

    type VARCHAR(20) DEFAULT NULL,
    txn_date VARCHAR(50) DEFAULT NULL,
    taxpayer_name VARCHAR(255) DEFAULT NULL,
    period VARCHAR(100) DEFAULT NULL,
    or_no VARCHAR(100) DEFAULT NULL,
    td_no VARCHAR(100) DEFAULT NULL,
    barangay_name VARCHAR(150) DEFAULT NULL,

    r1 DECIMAL(15,2) NOT NULL DEFAULT 0.00,
    r2 DECIMAL(15,2) NOT NULL DEFAULT 0.00,
    r3 DECIMAL(15,2) NOT NULL DEFAULT 0.00,
    r4 DECIMAL(15,2) NOT NULL DEFAULT 0.00,
    r5 DECIMAL(15,2) NOT NULL DEFAULT 0.00,
    r6 DECIMAL(15,2) NOT NULL DEFAULT 0.00,
    r7 DECIMAL(15,2) NOT NULL DEFAULT 0.00,
    r8 DECIMAL(15,2) NOT NULL DEFAULT 0.00,
    r9 DECIMAL(15,2) NOT NULL DEFAULT 0.00,
    r10 DECIMAL(15,2) NOT NULL DEFAULT 0.00,
    r11 DECIMAL(15,2) NOT NULL DEFAULT 0.00,
    r12 DECIMAL(15,2) NOT NULL DEFAULT 0.00,
    r13 DECIMAL(15,2) NOT NULL DEFAULT 0.00,
    r14 DECIMAL(15,2) NOT NULL DEFAULT 0.00,
    total DECIMAL(15,2) NOT NULL DEFAULT 0.00,

    row_hash CHAR(64) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_taxpayer_raw_batch
        FOREIGN KEY (batch_id) REFERENCES import_batches(id)
        ON DELETE CASCADE,

    KEY idx_batch_id (batch_id),
    KEY idx_taxpayer_name (taxpayer_name),
    KEY idx_period (period),
    KEY idx_or_no (or_no),
    KEY idx_td_no (td_no),
    KEY idx_barangay_name (barangay_name),
    KEY idx_row_hash (row_hash),

    UNIQUE KEY uq_batch_rowhash (batch_id, row_hash)
);

ALTER TABLE taxpayer_raw_imports DROP INDEX uq_batch_rowhash;

CREATE TABLE messages (
    id INT(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
    sender_role ENUM('ADMIN','TREASURY') NOT NULL,
    receiver_role ENUM('ADMIN','TREASURY') NOT NULL,
    message TEXT NOT NULL,
    is_read TINYINT(1) NOT NULL DEFAULT 0,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);


ALTER TABLE

ALTER TABLE alicia
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE cabugao
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE dagupan
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE diodol
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE dumabel
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE dungo
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE guinalbin
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE nagabgaban
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE palacian
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE pinaripad_norte
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE pinaripad_sur
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE progreso
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE ramos
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE rangayan
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE san_antonio
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE san_benigno
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE san_francisco
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE san_leonardo
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE san_manuel
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE san_ramon
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE victoria
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE villa_pagaduan
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE villa_santiago
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE villa_ventura
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;

ALTER TABLE ligaya
ADD COLUMN north VARCHAR(255) NULL AFTER cancellation,
ADD COLUMN east VARCHAR(255) NULL AFTER north,
ADD COLUMN south VARCHAR(255) NULL AFTER east,
ADD COLUMN west VARCHAR(255) NULL AFTER south,
ADD COLUMN ref_td VARCHAR(255) NULL AFTER west,
ADD COLUMN ref_pin VARCHAR(255) NULL AFTER ref_td,
ADD COLUMN beneficial_user VARCHAR(255) NULL AFTER ref_pin,
ADD COLUMN ass_level VARCHAR(255) NULL AFTER beneficial_user,
ADD COLUMN rec_app VARCHAR(255) NULL AFTER ass_level,
ADD COLUMN prev_td VARCHAR(255) NULL AFTER rec_app,
ADD COLUMN prev_ass VARCHAR(255) NULL AFTER prev_td,
ADD COLUMN prev_owner VARCHAR(255) NULL AFTER prev_ass,
ADD COLUMN memo TEXT NULL AFTER prev_owner;



add new column

ALTER TABLE alicia ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE cabugao ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE dagupan ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE diodol ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE dumabel ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE dungo ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE guinalbin ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE nagabgaban ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE palacian ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE pinaripad_norte ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE pinaripad_sur ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE progreso ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE ramos ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE rangayan ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE san_antonio ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE san_benigno ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE san_francisco ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE san_leonardo ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE san_manuel ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE san_ramon ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE victoria ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE villa_pagaduan ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE villa_santiago ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE villa_ventura ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;
ALTER TABLE ligaya ADD COLUMN app_by VARCHAR(255) NULL AFTER rec_app;